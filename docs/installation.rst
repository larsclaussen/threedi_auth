============
Installation
============

At the command line::

    $ easy_install threedi_auth

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv threedi_auth
    $ pip install threedi_auth
